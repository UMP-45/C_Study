
template<typename T> class element{ public: T val, *ptr; };

class vessel{
 private:
  element arr[];
 public:
  count(){ return &arr + sizeof(arr) - ; } 
  begin(){ return &arr;}
  end(){ begin() + count;}
};

#include<iostream>

int main(int argc, char *argv[]){
  node<int> obj;
  
  for(auto i = obj.begin(); i not_eq obj.end(); ++i)

return 0; }
